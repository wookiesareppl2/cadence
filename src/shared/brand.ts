// Single source of truth for the app's user-facing display name. Change it here to
// rebrand — every window title, dialog, splash wordmark, and version label reads
// from this. Note: the Electron appId, the package "name", and the release repo are
// intentionally NOT derived from this (they are stable identifiers, not branding).
export const APP_NAME = 'Cadence'
