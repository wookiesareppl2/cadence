---
description: Save this project to its Felix vault memory home or legacy Memory Bank for reliable cross-provider continuation
---

Load the `save` skill with OpenCode's native skill tool and follow it exactly. Treat the command
arguments only as its fidelity mode. When no argument is supplied, use the skill default.

Arguments: $ARGUMENTS

